from setuptools import setup, find_packages

setup(
    name="randocardrissian",
    author="Kenzie Togami",
    version="0.0.1",
    description="Rando Cardrissian Bot",
    packages=find_packages(exclude=['config'])
)
